# ============================================================
# Configuration — setup-claude-code-mobile-first
# ============================================================
# Copy this file to config.env and fill in your values:
#   cp config.env.example config.env
#   nano config.env
# ============================================================

# --- Required ---

# Anthropic API key (get one at https://console.anthropic.com)
ANTHROPIC_API_KEY="sk-ant-..."

# Password for web terminal (ttyd) and code-server (VS Code web)
WEB_PASSWORD="change-me-strong-password"

# --- Optional ---

# System user to configure (defaults to current user)
# WORK_USER="ubuntu"

# Project directory where Claude Code agents will work
# PROJECT_DIR="$HOME/workspace/my-project"

# Tailscale auth key (get one at https://login.tailscale.com/admin/settings/keys)
# Required only if you want VPN access
# TAILSCALE_AUTH_KEY="tskey-auth-..."

# Domain for HTTPS (Caddy auto-TLS). Leave empty for IP-only access.
# DOMAIN="claude.example.com"

# code-server bind address (default: 0.0.0.0:8080)
# CODE_SERVER_BIND="0.0.0.0:8080"

# ttyd bind port (default: 7681)
# TTYD_PORT="7681"

# --- Azure (only if using terraform/) ---

# ARM_CLIENT_ID=""
# ARM_TENANT_ID=""
# ARM_SUBSCRIPTION_ID=""
# ARM_CLIENT_SECRET=""
# AZURE_REGION="uksouth"
# VM_SIZE="Standard_B2s"
# VM_ADMIN_USER="azureuser"
